import { localize, type Language, type PageSlug, pages, site } from "../content/site";
export function SiteHeader({language,page,onLanguageChange}:{language:Language;page:PageSlug;onLanguageChange:(language:Language)=>void}){return <header className="site-header"><a className="wordmark" href="#home">{site.name}</a><nav aria-label="Primary navigation">{pages.map(item=><a key={item.slug} className={item.slug===page?"active":""} href={`#${item.slug}`}>{localize(item.label,language)}</a>)}</nav><div className="language-switcher" aria-label="Language"><button className={language==="en"?"selected":""} onClick={()=>onLanguageChange("en")}>EN</button><span>/</span><button className={language==="zh"?"selected":""} onClick={()=>onLanguageChange("zh")}>中文</button></div></header>}
