import { localize, type Language, type PageSlug, pages, site } from "../content/site";
export function SiteHeader({language,page,onLanguageChange}:{language:Language;page:PageSlug;onLanguageChange:(language:Language)=>void}){return <header className="site-header"><div className="site-header-inner"><a className="wordmark" href="#home">{site.name}</a><nav aria-label="Primary navigation">{pages.map(item=><a key={item.slug} className={item.slug===page?"active":""} aria-current={item.slug===page?"page":undefined} href={`#${item.slug}`}>{localize(item.label,language)}</a>)}</nav><div className="language-switcher" aria-label="Language"><button className={language==="en"?"selected":""} onClick={()=>onLanguageChange("en")} aria-pressed={language==="en"}>EN</button><span>/</span><button className={language==="zh"?"selected":""} onClick={()=>onLanguageChange("zh")} aria-pressed={language==="zh"}>中文</button></div></div></header>}
